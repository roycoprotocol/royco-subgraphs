# prettier-ignore
specVersion: 1.3.0
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum
    name: BaseVault
    network: {{network}}
    source:
      address: "{{stakedRoyusdContractAddress}}"
      abi: BaseVault
      startBlock: {{stakedRoyusdStartBlock}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.9
      language: wasm/assemblyscript
      entities:
        - Transfer
        - UnstakingCommenced
        - AssetsUnstaked
      abis:
        - name: BaseVault
          file: ./abis/BaseVault.json
      eventHandlers:
        - event: Transfer(indexed address,indexed address,uint256)
          handler: handleTransfer
      file: ./src/vault.ts
