Royco Day ABIs and `contracts/` are sourced from
`royco-day@8d9b135b97259f344194cddb0abc2c85f985030f`.

`RoycoDayKernel.json` uses the concrete `RoycoDayBalancerV3Kernel` ABI so the
Balancer venue events are included. `latest/` mirrors the active Royco ABIs.
